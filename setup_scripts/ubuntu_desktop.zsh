#! /bin/zsh

# ########################################################
# # PREAMBLE | DO NOT REMOVE
# /bin/zsh -c "$(curl -fsSL https://raw.githubusercontent.com/aegatlin/setup/master/bootstrap/create_bootstrap.zsh)"
# source ./bootstrap.zsh
# # PREAMBLE | DO NOT REMOVE
# ########################################################

# # For the moment, order matters
# tools=(
#   zsh
#   apt
#   snap
#   tmux
#   vim
# )

# load $tools

# For desktop
############

# sudo snap install code --classic
# sudo snap install heroku --classic
# sudo snap install vlc
# sudo snap install postman

# Missing in the minimal install
# Needed to create bootup disk images
# and needed to display certain emojis, respectively
# sudo apt install usb-creator-gtk
# sudo apt install fonts-noto-color-emoju
